resources:
  jobs:
    extract-job:
      name: BDS-QA-EXTRACT-JOB-MODULE
      job_clusters:
        - job_cluster_key: DBX-BDS-QA-BUNDLE-CLUSTER
          new_cluster:
            num_workers: 1
            spark_version: 14.3.x-scala2.12
            node_type_id: Standard_DS3_v2
      tasks:
        - task_key: Extract
          job_cluster_key: DBX-BDS-QA-BUNDLE-CLUSTER
          notebook_task:
            path: ../src/main/extract.py
